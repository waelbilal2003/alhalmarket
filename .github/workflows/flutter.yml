name: Flutter CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Flutter Clean
        run: flutter clean

      - name: Flutter Pub Get
        run: flutter pub get

      - name: Build APKs (Split per ABI)
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
        run: |
          mkdir -p android/app
          echo "$KEYSTORE_BASE64" | base64 --decode > android/app/upload-keystore.jks
          flutter build apk --release --split-per-abi

      - name: Upload 32-bit APK (armeabi-v7a)
        uses: actions/upload-artifact@v4
        with:
          name: release-apk-armeabi-v7a
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk

      - name: Upload 64-bit APK (arm64-v8a)
        uses: actions/upload-artifact@v4
        with:
          name: release-apk-arm64-v8a
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Flutter Clean
        run: flutter clean

      - name: Flutter Pub Get
        run: flutter pub get

      - name: Setup Visual Studio C++ Build Tools
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install VC++ Redistributable
        run: |
          Invoke-WebRequest -Uri "https://aka.ms/vs/17/release/vc_redist.x64.exe" -OutFile "vc_redist.x64.exe"
          Start-Process -FilePath "./vc_redist.x64.exe" -ArgumentList "/install", "/quiet", "/norestart" -Wait
        shell: powershell

      - name: Build Windows Release
        run: flutter build windows --release

      # 🔍 خطوة تصحيح مؤقتة: نعرض محتويات المجلدات لمعرفة أين توجد الملفات
      - name: Debug Folder Structure
        run: |
          Write-Host "🔍 مجلد build/windows:"
          Get-ChildItem -Path "build/windows" -Recurse | Select-Object FullName
          
          Write-Host "🔍 بحث عن ملفات DLL:"
          Get-ChildItem -Path "build" -Include *.dll -Recurse | Select-Object FullName
        shell: powershell

      # ✅ الحل الذكي: نبحث عن الملفات في كل المسارات المحتملة
      - name: Ensure All Required DLLs Exist
        run: |
          $releaseDir = ""
          $potentialPaths = @(
            "build/windows/x64/runner/Release",
            "build/windows/runner/Release",
            "build/windows/x64/windows/runner/Release"
          )
          
          # نبحث عن المسار الصحيح الذي يحتوي على .exe
          foreach ($path in $potentialPaths) {
            if (Test-Path "$path/*.exe") {
              $releaseDir = $path
              break
            }
          }
          
          if ([string]::IsNullOrEmpty($releaseDir)) {
            Write-Host "❌ لم يتم العثور على مجلد Release!"
            exit 1
          }
          
          Write-Host "✅ سيتم استخدام المسار: $releaseDir"
          
          # 1. نسخ ملفات VC++ الأساسية (إذا لم تكن موجودة)
          $vcFiles = @("msvcp140.dll", "vcruntime140.dll", "vcruntime140_1.dll")
          foreach ($file in $vcFiles) {
            $destPath = "$releaseDir\$file"
            if (!(Test-Path $destPath)) {
              $sourcePath = "C:\Windows\System32\$file"
              if (Test-Path $sourcePath) {
                Write-Host "نسخ $file من System32"
                Copy-Item $sourcePath -Destination $releaseDir -Force
              } else {
                Write-Host "⚠️ تحذير: $file غير موجود في System32"
              }
            } else {
              Write-Host "✅ $file موجود مسبقاً"
            }
          }
          
          # 2. البحث عن ملفات plugins في كل المسارات
          $pluginFiles = @(
            @{Name="pdfium.dll";         SearchPath="printing"},
            @{Name="printing_plugin.dll"; SearchPath="printing"},
            @{Name="sqlite3.x64.windows.dll"; SearchPath="sqlite3"}
          )
          
          foreach ($plugin in $pluginFiles) {
            $found = $false
            $searchPaths = @(
              "$releaseDir\$($plugin.Name)",
              "build/**/$($plugin.SearchPath)/**/$($plugin.Name)",
              "build/**/$($plugin.Name)"
            )
            
            foreach ($searchPath in $searchPaths) {
              $files = Get-ChildItem -Path $searchPath -Recurse -ErrorAction SilentlyContinue
              if ($files) {
                foreach ($file in $files) {
                  Write-Host "نسخ $($plugin.Name) من $($file.FullName)"
                  Copy-Item $file.FullName -Destination $releaseDir -Force
                  $found = $true
                }
                break
              }
            }
            
            if (-not $found) {
              Write-Host "⚠️ تحذير: $($plugin.Name) غير موجود - قد لا يعمل التطبيق بشكل كامل"
            }
          }
        shell: powershell

      - name: Package Windows App as ZIP
        run: |
          $releaseDir = ""
          $potentialPaths = @(
            "build/windows/x64/runner/Release",
            "build/windows/runner/Release",
            "build/windows/x64/windows/runner/Release"
          )
          
          foreach ($path in $potentialPaths) {
            if (Test-Path "$path/*.exe") {
              $releaseDir = $path
              break
            }
          }
          
          if ([string]::IsNullOrEmpty($releaseDir)) {
            Write-Host "❌ لم يتم العثور على مجلد Release!"
            exit 1
          }
          
          $zipPath = "windows-app.zip"
          Compress-Archive -Path "$releaseDir/*" -DestinationPath $zipPath -Force
          Write-Host "✅ تم إنشاء الملف: $zipPath"
        shell: powershell

      - name: Upload ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-windows-x64-zip
          path: windows-app.zip